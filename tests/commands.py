"""

Commands are things that can be executed to get things done. A command
will bind a python function to a file on how to use it, and display this
usage information in Intermediate JSON Schema Format so that the local
method of display can be used

The command module contains methods of creating and loading commands using a
custom TOML file along with the definition of the command object itself
"""

import unittest

class TestJSONInterchangeSchema:
    """
    The JSON interchange schema format is used to ensure it is possible to
    define a schema then redefine it to whatever the local model will best
    accept.

    The design is, basicallly,
    """

class TestCommand(unittest.TestCase):
    """
    A command has the object


    """

    def test_simple_command(self):
